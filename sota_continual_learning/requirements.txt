# Core ML framework (used everywhere)
torch>=2.0.0

# Data processing
scikit-learn>=1.5.0
numpy>=1.26.0
pandas>=2.0.0
pyarrow>=14.0.0

# Plotting (monitor.py only)
matplotlib>=3.9.0

# GPU / process monitoring
psutil>=6.0.0

# Note: argparse, logging, pathlib, random, typing are part of the Python stdlib.
